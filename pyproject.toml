[project]
name = "fly-in"
version = "0.1.0"
description = "Grafo, oggetti e multithreading"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.13.4",
    "pygame>=2.6.1",
]
