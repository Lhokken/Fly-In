[project]
name = "fly-in"
version = "0.1.0"
description = "gestione grafo a oggetti"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
    "torch>=2.0.0",
    "transformers>=4.40.0",
    "huggingface-hub>=0.20.0",
    "python-dotenv>=1.0.0",
    "accelerate>=0.30.0",
    "fire>=0.7.1",
]

# [build-system]
# requires = ["hatchling"]
# build-backend = "hatchling.build"

# [tool.hatch.build.targets.wheel]
# packages = ["fly_in_pak"]
